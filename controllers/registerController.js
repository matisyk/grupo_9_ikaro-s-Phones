const path = require("path");

const registerController ={
    register:(req, res) => {
        res.render('register');
    },
    guardar: (req, res) =>{
        
    }
}

module.exports= registerController;